{
  "name": "ftn-go-final-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ftn-go-final-test",
    "create-local": "graph create --node http://localhost:8020/ ftn-go-final-test",
    "remove-local": "graph remove --node http://localhost:8020/ ftn-go-final-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ftn-go-final-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
